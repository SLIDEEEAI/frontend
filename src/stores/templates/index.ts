import common from './slideTemplates/common';
import textFullWidth from './slideTemplates/textFullWidth';
import textWithPictureFullWidth from './slideTemplates/textWithPictureFullWidth';

export default {
    [common.name] : common,
    [textFullWidth.name] : textFullWidth,
    [textWithPictureFullWidth.name] : textWithPictureFullWidth
}